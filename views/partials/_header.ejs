<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
      </div>
    </div>
    <% if(user){%>
    <!-- AS"PFGINHSD"OIFNSD"LNFSD"LJFNSD"LJFNSDF -->
    <p style="margin-right: 15px;" class="navbar-text">Logged in as: <%=user['email']%>
    </p>
    <form action="/logout" method="POST">
      <button type='submit' class="btn btn-outline-secondary">Logout</button>
    </form>
    <% } else{ %>
    <form action="/login" method="POST">
      <input class="nav-item nav-link" type="test" name="username" id="username" placeholder="username">
      <button style="margin: 10px;" type="submit" class="btn btn-outline-secondary">Login</button>
    </form>
    <% } %>

  </nav>
</header>



<!-- 
In the _header.ejs partial of your app, create a <form> that POSTs to /login.
  The form should contain:
  
  a single text input with name="username"
  a submit button to submit the form
  Users will type in any username into this form in order to "sign in" for now.
  Since the POST route does not exist yet, we expect that submission of the form will result in a server error. -->